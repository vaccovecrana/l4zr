{
    // Runs Gradle with the system-installed Gradle (or from PATH inside devcontainer)
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gradle:build",
            "type": "shell",
            "command": "gradle",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gradle"
            ]
        }
    ]
}